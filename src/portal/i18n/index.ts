import en from "./en"
import zh from "./zh"

export default {
    "locale" : "zh",
    "en" : en,
    "zh" : zh,
    /*messeges : {[key: string]:any} = {
        "en":en
    };
    message(key : string) : string {
        return eval("this.messeges[this.locale]['" + key.replace(/\./g, "']['") + "']");
    }*/
    
}